# Rust Animation Reference Ports

These are the original Rust sources from `donk-cli/crates/donk-anim/src`.
They are preserved here for algorithmic reference during Go porting.

Do not import these files into the Go build. Port needed effects into Go
packages under `internal/ui/anim/` or `internal/ui/anim/scenes/`.

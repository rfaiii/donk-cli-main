# yaml-language-server: $schema=https://goreleaser.com/static/schema-pro.json
# vim: set ts=2 sw=2 tw=0 fo=jcroql
version: 2

project_name: bvr-cli

includes:
  - from_url:
      url: charmbracelet/meta/main/notarize.yaml

metadata:
  mod_timestamp: "{{ .CommitTimestamp }}"
  license: "FSL-1.1-MIT"
  homepage: "https://github.com/richavery/bvr-cli"
  description: "BVR is a keyboard-first terminal AI workspace for projects, local models, tools, and automation."
  full_description: "BVR is a keyboard-first terminal AI workspace for projects, local models, tools, and automation."
  maintainers:
    - "kujtimiihoxha <kujtim@charm.sh>"
    - "caarlos0 <carlos@charm.sh>" # for aur

nightly:
  publish_release: true
  keep_single_release: true
  version_template: "{{ incminor .Version }}-nightly"

snapshot:
  version_template: "0.0.0-{{ .Timestamp }}"

before:
  hooks:
    - go mod tidy
    - rm -rf completions
    - mkdir completions
    - rm -rf manpages
    - mkdir manpages
      - sh -c 'go run . completion bash >./completions/bvr-cli.bash'
      - sh -c 'go run . completion zsh >./completions/bvr-cli.zsh'
      - sh -c 'go run . completion fish >./completions/bvr-cli.fish'
      - sh -c 'go run . man | gzip -c >./manpages/bvr-cli.1.gz'

# gomod:
#   proxy: true

builds:
  - env:
      - CGO_ENABLED=0
      - GOEXPERIMENT=greenteagc
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: darwin
        goarch: arm
    ldflags:
      - -s -w -X github.com/richavery/bvr-cli/internal/version.Version={{.Version}}
    flags:
      - -trimpath

archives:
  - name_template: >-
      bvr-cli_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- with .Arm}}v{{ . }}{{ end }}
    wrap_in_directory: true
    files:
      - README*
      - docs/installation.md
      - LICENSE*
      - manpages/*
      - completions/*
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

aur_sources:
  - private_key: "{{ .Env.AUR_KEY }}"
    disable: "{{ with .Prerelease }}true{{ end }}"
    git_url: "ssh://aur@aur.archlinux.org/bvr-cli.git"
    commit_author:
      name: "BVR"
      email: "bvrcli@users.noreply.github.com"
    makedepends:
      - go
      - git
    prepare: |-
      go mod download
    build: |-
      mkdir completions
      mkdir manpages
      export CGO_CPPFLAGS="${CPPFLAGS}"
      export CGO_CFLAGS="${CFLAGS}"
      export CGO_CXXFLAGS="${CXXFLAGS}"
      export CGO_LDFLAGS="${LDFLAGS}"
      export GOFLAGS="-buildmode=pie -trimpath -mod=readonly -modcacherw"
      go build -ldflags="-w -s -buildid='' -linkmode=external -X github.com/richavery/bvr-cli/internal/version.Version=v${pkgver}" .
      ./bvr-cli completion bash >./completions/bvr-cli.bash
      ./bvr-cli completion zsh >./completions/bvr-cli.zsh
      ./bvr-cli completion fish >./completions/bvr-cli.fish
      ./bvr-cli man | gzip -c >./manpages/bvr-cli.1.gz
    package: |-
      # bin
      install -Dm755 "./bvr-cli" "${pkgdir}/usr/bin/bvr-cli"
      # license
      mkdir -p "${pkgdir}/usr/share/licenses/bvr-cli/"
      install -Dm644 ./LICENSE* "${pkgdir}/usr/share/licenses/bvr-cli/"
      # completions
      mkdir -p "${pkgdir}/usr/share/bash-completion/completions/"
      mkdir -p "${pkgdir}/usr/share/zsh/site-functions/"
      mkdir -p "${pkgdir}/usr/share/fish/vendor_completions.d/"
      install -Dm644 "./completions/bvr-cli.bash" "${pkgdir}/usr/share/bash-completion/completions/bvr-cli"
      install -Dm644 "./completions/bvr-cli.zsh" "${pkgdir}/usr/share/zsh/site-functions/_bvr-cli"
      install -Dm644 "./completions/bvr-cli.fish" "${pkgdir}/usr/share/fish/vendor_completions.d/bvr-cli.fish"
      # man pages
      install -Dm644 "./manpages/bvr-cli.1.gz" "${pkgdir}/usr/share/man/man1/bvr-cli.1.gz"
      # readme
      mkdir -pv "${pkgdir}/usr/share/doc/bvr-cli/"
      install -Dm644 README* "${pkgdir}/usr/share/doc/bvr-cli/"

aurs:
  - private_key: "{{ .Env.AUR_KEY }}"
    disable: "{{ with .Prerelease }}true{{ end }}"
    git_url: "ssh://aur@aur.archlinux.org/bvr-cli-bin.git"
    commit_author:
      name: "BVR"
      email: "bvrcli@users.noreply.github.com"
    provides:
      - bvr-cli
    conflicts:
      - bvr-cli
    package: |-
      case "$CARCH" in
        aarch64)
          cd "${srcdir}/bvr-cli_${pkgver}_Linux_arm64"
          ;;
        *)
          cd "${srcdir}/bvr-cli_${pkgver}_Linux_${CARCH}"
          ;;
      esac
      # bin
      install -Dm755 "./bvr-cli" "${pkgdir}/usr/bin/bvr-cli"
      # license
      mkdir -p "${pkgdir}/usr/share/licenses/bvr-cli/"
      install -Dm644 ./LICENSE* "${pkgdir}/usr/share/licenses/bvr-cli/"
      # completions
      mkdir -p "${pkgdir}/usr/share/bash-completion/completions/"
      mkdir -p "${pkgdir}/usr/share/zsh/site-functions/"
      mkdir -p "${pkgdir}/usr/share/fish/vendor_completions.d/"
      install -Dm644 "./completions/bvr-cli.bash" "${pkgdir}/usr/share/bash-completion/completions/bvr-cli"
      install -Dm644 "./completions/bvr-cli.zsh" "${pkgdir}/usr/share/zsh/site-functions/_bvr-cli"
      install -Dm644 "./completions/bvr-cli.fish" "${pkgdir}/usr/share/fish/vendor_completions.d/bvr-cli.fish"
      # man pages
      install -Dm644 "./manpages/bvr-cli.1.gz" "${pkgdir}/usr/share/man/man1/bvr-cli.1.gz"
      # readme
      mkdir -pv "${pkgdir}/usr/share/doc/bvr-cli/"
      install -Dm644 README* "${pkgdir}/usr/share/doc/bvr-cli/"

furies:
  - disable: "{{ if (or .Prerelease .IsNightly) }}true{{ end }}"
    account: "{{ with .Env.FURY_TOKEN }}bvrcli{{ else }}{{ end }}"
    secret_name: FURY_TOKEN

brews:
  - repository:
      owner: richavery
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://bvr-cli.com"
    description: "BVR is a keyboard-first terminal AI workspace."
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    commit_author:
      name: "BVR"
      email: "bvrcli@users.noreply.github.com"
    goarm: 7
    extra_install: |-
      bash_completion.install "completions/{{ .ProjectName }}.bash" => "{{ .ProjectName }}"
      zsh_completion.install "completions/{{ .ProjectName }}.zsh" => "_{{ .ProjectName }}"
      fish_completion.install "completions/{{ .ProjectName }}.fish"
      man1.install "manpages/{{ .ProjectName }}.1.gz"

scoops:
  - repository:
      owner: richavery
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    description: "BVR is a keyboard-first terminal AI workspace."
    homepage: "https://bvr-cli.com"
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    commit_author:
      name: "BVR"
      email: "bvrcli@users.noreply.github.com"

npms:
  - name: "@bvrcli/bvr-cli"
    repository: "git+https://github.com/richavery/bvr-cli.git"
    bugs: https://github.com/richavery/bvr-cli/issues
    access: public
    disable: "{{ with .Prerelease }}true{{ end }}"

nfpms:
  - formats:
      - apk
      - deb
      - rpm
      - archlinux
    file_name_template: "{{ .ConventionalFileName }}"
    contents:
      - src: ./completions/bvr-cli.bash
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files/usr{{ end }}/etc/bash_completion.d/bvr-cli'
      - src: ./completions/bvr-cli.fish
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files{{ end }}/usr/share/fish/vendor_completions.d/bvr-cli.fish'
      - src: ./completions/bvr-cli.zsh
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files{{ end }}/usr/share/zsh/site-functions/_bvr-cli'
      - src: ./manpages/bvr-cli.1.gz
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files{{ end }}/usr/share/man/man1/bvr-cli.1.gz'
    rpm:
      signature:
        key_file: '{{ if ne (index .Env "GPG_KEY_PATH") "" }}{{ .Env.GPG_KEY_PATH }}{{ else }}{{ end }}'
    deb:
      signature:
        key_file: '{{ if ne (index .Env "GPG_KEY_PATH") "" }}{{ .Env.GPG_KEY_PATH }}{{ else }}{{ end }}'

signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

source:
  enabled: true

sboms:
  - artifacts: archive
  - id: source
    artifacts: source

nix:
  - repository:
      owner: "charmbracelet"
      name: nur
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: "BVR"
      email: "bvrcli@users.noreply.github.com"
    license: fsl11Mit
    formatter: nixfmt
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    extra_install: |-
      installManPage ./manpages/bvr-cli.1.gz
      installShellCompletion ./completions/*

winget:
  - publisher: bvr-cli
    copyright: BVR, Inc
    publisher_url: https://github.com/richavery/bvr-cli
    release_notes_url: "https://github.com/richavery/bvr-cli/releases/tag/{{.Tag}}"
    license_url: https://github.com/richavery/bvr-cli/blob/main/LICENSE.md
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    commit_author:
      name: "BVR"
      email: "bvrcli@users.noreply.github.com"
    repository:
      owner: "charmbracelet"
      name: winget-pkgs
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
      branch: "bvr-cli-{{.Version}}"
      pull_request:
        enabled: true
        draft: false
        check_boxes: true
        base:
          owner: microsoft
          name: winget-pkgs
          branch: master
        body: |
          /cc @andreynering

changelog:
  sort: asc
  disable: "{{ .IsNightly }}"
  use: github
  filters:
    exclude:
      - "^(build|ci): "
      - "^chore: auto-update generated files$"
      - "^chore: docs$"
      - "^chore: schema update$"
      - "^chore: schema$"
      - "^chore: typo$"
      - "^chore: update schema$"
      - "^chore: update$"
      - "^chore\\(legal\\): "
      - "^chore\\(deps\\): "
      - "^docs: update$"
      - "^test:"
      - "^test\\("
      - "^v\\d.*"
      - "merge conflict"
      - "merge conflict"
      - Merge branch
      - Merge pull request
      - Merge remote-tracking branch
      - go mod tidy
      - "^wip "
      - "^wip:"
  groups:
    - title: "Deps"
      regexp: "^.*\\(deps\\)*:+.*$"
      order: 300
    - title: "New!"
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 100
    - title: "Fixed"
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 200
    - title: "Docs"
      regexp: "^.*docs[(\\w)]*:+.*$"
      order: 400
    - title: "Other stuff"
      order: 9999

release:
  prerelease: auto
  footer:
    from_url:
      url: https://raw.githubusercontent.com/charmbracelet/meta/main/footer.md
